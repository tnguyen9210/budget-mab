defaults:
  - exp_schema
  - _self_
  - override hydra/job_logging: disabled
  - override hydra/hydra_logging: disabled

hydra:
  output_subdir: null
  run:
    dir: .

# Same as default.yaml but homogeneous regime only.
name: homogeneous
K: 10
n_seeds: 500
regimes: [homogeneous]
instance_seed: 42
n_workers: 96
